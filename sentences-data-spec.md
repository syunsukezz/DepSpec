# タイピングデータ仕様（sentences.json）

このドキュメントは、keywave（本リポジトリ）が読み込むタイピングデータファイル
`src/data/sentences.json` のフォーマット仕様です。**別プロジェクトとして開発する
GUIエディタ**がこのファイルを読み書きする際の一次情報として作成しています。

実装の正本は `src/sentences.ts`（読み込み・検証・組み立てロジック）と
`src/keygraph.js`（タイピング判定エンジン、使用可能な文字集合の定義元）です。
本ドキュメントとソースコードが食い違う場合はソースコードを優先してください。

## 1. 背景・目的

keywave は、フレーズ（お題文）をタイプするタイピングゲームです。各フレーズは

- タイプ対象の「かな」文字列（ローマ字入力に変換される）
- プレイヤーが読む「漢字混じり文」（表示専用、タイピングには使わない）
- 一部の単語に付ける「打鍵圧の指示」（強く／普通に／弱く）

を持ちます。これら3つはそれぞれ独立した文字列ではなく、**「セグメント」（かな＋
対応する漢字＋打鍵圧指示）の配列**として1つのフレーズを表現します。セグメント単位
で持つことで、打鍵圧マークをローマ字入力欄だけでなく漢字混じり文の正しい位置にも
表示できます。

## 2. ファイルの場所と読み込まれ方

- パス: `src/data/sentences.json`（このリポジトリのルートから）
- 本体アプリ（Vite）はビルド時にこのJSONを**静的import**します（`src/sentences.ts`
  1行目付近の `import sentencesData from './data/sentences.json'`）。実行時に
  fetch する形ではないため、**このファイルを書き換えたら本体アプリの再ビルド
  （`npm run dev` の再起動 or `npm run build`）が必要**です。
- GUIエディタは、このファイルをそのまま読み込んで編集し、保存時にこのファイルへ
  上書きする運用を想定しています。

## 3. トップレベル構造

```ts
type SentencesJson = {
  Easy: Phrase[];
  Normal: Phrase[];
  Hard: Phrase[];
};
```

- トップレベルはオブジェクトで、キーは必ず `"Easy"` / `"Normal"` / `"Hard"` の3つ
  （大文字小文字も含め完全一致、過不足なし）。
- 各値はフレーズの配列。**出題順はこの配列の並び順**（ランダムではない。詳細は
  [9. レベルと出題順](#9-レベルの目安と台本の連続性ルール) を参照）。
- 各配列は空でも技術的にはエラーにならないが、空だとそのレベルは出題不能になる。

## 4. フレーズ / セグメントのフィールド定義

```ts
type Phrase = {
  segments: Segment[];
};

type Segment = {
  kanji: string;                                   // 漢字混じり文の対応部分（表示専用）
  kana: string;                                     // 対応するひらがな読み（タイピング対象）
  mark: "strong" | "normal" | "weak" | null;        // 打鍵圧の指示（無指定は null）
};
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `Phrase.segments` | `Segment[]` | ○（1件以上） | フレーズを構成するセグメントの配列。左から右へ読む順に並べる |
| `Segment.kanji` | `string` | ○（空文字列も可） | 漢字混じり文のうち、このセグメントに対応する部分。表示専用 |
| `Segment.kana` | `string` | ○（空文字列も可） | `kanji` と同じ内容のひらがな読み。実際にタイプさせる文字列 |
| `Segment.mark` | `"strong" \| "normal" \| "weak" \| null` | ○ | このセグメントの打鍵圧指示。詳細は [6.](#6-mark打鍵圧指定の意味) |

## 5. セグメントの連結ルール

同じフレーズ内のセグメントを配列の順番どおりに連結すると、フレーズ全体の文字列
になります。

- `text`（タイピング対象の全文） = 全セグメントの `kana` を順に連結したもの
- `displayText`（漢字混じり表示文の全文） = 全セグメントの `kanji` を順に連結したもの

**`kanji` と `kana` は同じ意味内容・同じ順序を指すように対応させてください。**
1セグメントの区切りは「単語」単位を目安にします（文節より細かくてOK）。

例:

```json
[
  { "kanji": "企画部の", "kana": "きかくぶの", "mark": "normal" },
  { "kanji": "吉田",     "kana": "よしだ",     "mark": "strong" },
  { "kanji": "から",     "kana": "から",       "mark": null }
]
```

→ `text` = `"きかくぶのよしだから"` 、 `displayText` = `"企画部の吉田から"`
（「吉田」の部分だけ、ローマ字入力欄・漢字混じり文の両方で強く打つマークが表示される）

セグメントの区切り自体に技術的な制約はありませんが、`kanji` と `kana` の対応が
ズレている（区切り位置や順序が食い違っている）と、打鍵圧マークが漢字混じり文の
誤った位置に表示されてしまいます。**この整合性はプログラムでは検証できないため、
人間（GUIエディタの利用者）が目視で確認する必要があります。**
（[11. バリデーション](#11-バリデーション) も参照）

## 6. mark（打鍵圧指定）の意味

| 値 | 意味 | ローマ字欄・漢字混じり文でのアイコン表示 | クリア条件（アナログモードのスコア判定） |
|---|---|---|---|
| `"strong"` | 強く打つ指示 | ▲（赤） | 打鍵圧が「弱い」でなければクリア（普通 or 強いでOK） |
| `"weak"` | 弱く打つ指示 | ▼（青） | 打鍵圧が「強い」でなければクリア（弱い or 普通でOK） |
| `"normal"` | 普通に打つ指示 | なし（無印。ただしスコア判定の対象にはなる） | 打鍵圧が「普通」のときのみクリア |
| `null` | 指示なし | なし | 判定対象外（何を打っても影響しない） |

- アイコンが表示される（プレイヤーに見える指示）のは `"strong"` / `"weak"` のみです。
  `"normal"` は見た目には出ませんが、スコア判定の対象になる点が `null` と異なります
  （`"normal"` を付けた区間は「普通の強さ」で打たないと表現点のボーナスが得られない）。
- 通常キーボードモード（アナログキーボード非接続時）では打鍵圧そのものを検出でき
  ないため、`mark` はローマ字欄・漢字混じり文の表示（フォント切り替え等）には影響
  しますが、実質的なスコア判定は行われません。

### 運用の推奨

- 1フレーズにつき、強調したい単語は **1〜2個程度**に絞ってください。マークだらけ
  にすると「強調」の意味が薄れます。
- 新しいJSON形式では、セグメント同士は自由に組み合わせられます。`mark: null` の
  セグメントと `mark: "strong"` 等のセグメントを任意の順で混在させて構いません
  （旧TS文字列形式にあった「マーク区間の直前を埋め草として `/M` で閉じる必要が
  ある」という制約は、この形式では存在しません）。

## 7. かな（`kana`）フィールドの文字制約

`kana` はタイピング判定エンジン `keygraph`（`src/keygraph.js`）にそのまま渡されます。
**`keygraph` が認識できる文字だけ**を使用してください。認識できない文字が含まれる
文字列を `keygraph.build()` に渡すと、ブラウザのコンソールに warning が出るだけで
例外にはならず、**その文字がゲーム内で永久にタイプ不能になります**（GUIエディタ
側で保存前に検証することを強く推奨します。[11.](#11-バリデーション) 参照）。

使用可能な文字（`src/keygraph.js` の `_char_keys_table` に基づく）:

- ひらがな全般: 清音・濁音・半濁音・拗音（きゃ等）・小さい母音（ぁぃぅぇぉ）・
  小さい「っ」「ゃゅょ」「ゎ」「ヵ」「ヶ」・「ん」
- 長音記号「ー」
- カタカナは **「ヴ」のみ**対応（他のカタカナは非対応。カタカナ語はひらがな読み
  にしてください。例: 「ペン」→ `"ぺん"`）
- 半角数字 `0`〜`9`
- 半角英小文字 `a`〜`z`（大文字は非対応。英単語は小文字で書く。例: 「IMU」→ `"imu"`）

使用できない文字（一部の記号は `keygraph` 自体は対応していますが、既存データの
慣例として使用していません）:

- 漢字・全角記号（句読点「、」「。」等）・大文字アルファベット・カタカナ（「ヴ」を
  除く）
- これらが元の台詞に含まれる場合は、句読点は除去し、カタカナ・漢字・英単語は
  ひらがな読みに変換してください。

## 8. 漢字（`kanji`）フィールドの制約

`kanji` は表示専用で `keygraph` を通らないため、技術的な文字制約はありません
（漢字・カタカナ・全角英数字なども表示上は問題なく描画されます）。ただし、既存
データの慣例として **句読点・記号は使用しない**運用にしています（`kana` 側の
読みには句読点が無いため、`kanji` 側だけ句読点があると却って不自然に見えるため）。

## 9. レベルの目安と台本の連続性ルール

| レベル | 目安の長さ（1フレーズ＝1ラウンド） |
|---|---|
| `Easy` | 短いフレーズ中心。4〜15文字程度 |
| `Normal` | Easyより長い1文程度。12〜22文字程度 |
| `Hard` | Normalよりさらに長い1文。20〜40文字程度 |

（文字数は `text`＝かな全文の文字数の目安）

出題は各レベルの配列に並んだ順番どおりに行われ、末尾まで行くと先頭に戻ります
（ランダム出題ではありません）。これは元々、台本（会議・セール・授業などの場面）
の流れを保つための設計です。台本ベースで新しいフレーズを追加する場合は、次の
ルールを守ってください:

- 1ラウンド（1フレーズ）の中身は、**単一の「元文」**（台本の1文、句点や言い切りの
  形で終わる単位）に由来する文字列だけにする。
- 元文が長すぎる場合は単語の境目で複数ラウンドに分割してよいが、分割した末尾の
  ラウンドに**別の（次の）元文の先頭を混ぜてはいけない**。
- 同じ元文が長さの都合で複数ラウンドに分かれている場合、それらを（Hardなどで）
  1ラウンドに結合するのは良いが、**別の元文どうしを結合してはいけない**。

台本に基づかない単発の練習フレーズを追加する場合はこの限りではありません。

## 10. 数字・単位・記号の扱い

- 数字は読み仮名ではなく、**数字そのもの（半角数字）**を `kana` に書きます。
  `kanji` 側も同じ数字表記にします。
  - 例: `kana: "20"`, `kanji: "20"`
- 単位・助数詞（円／名／人／万／ヶ月／年／パーセントなど）は数字ではなく「語」
  なので、ひらがな読みのままで構いません。
  - 例: 「20名」→ `kana: "20めい"` ／「3ヶ月」→ `kana: "3かげつ"` ／
    「40%」→ `kana: "40ぱーせんと"`
- 句読点・記号は使用しません（[7.](#7-かなkanaフィールドの文字制約) [8.](#8-漢字kanjiフィールドの制約) 参照）。

## 11. バリデーション

### ゲーム側（`src/sentences.ts` の `validateSentencesData`）が実行時に検証する内容

これらに違反すると、ゲーム起動時に明確なエラーメッセージとともに例外が投げられ、
アプリが起動しません（フェイルファスト）。

1. トップレベルがオブジェクトであること
2. `Easy` / `Normal` / `Hard` がそれぞれ配列であること
3. 各フレーズが `segments` 配列（1要素以上）を持つこと
4. 各セグメントの `kanji` / `kana` が文字列であること
5. `mark` が `null` または `"strong"` / `"normal"` / `"weak"` のいずれかであること

### ゲーム側では検証されないため、GUIエディタ側で保存前にチェックすることを推奨する内容

上記5項目は構造的な最低限のチェックに過ぎません。以下はゲーム側では検証されず、
壊れたデータのまま保存すると「起動はするが挙動がおかしい／一部の文字がタイプ
不能」といった問題が実行時まで気づかれない可能性があります。

- `kana` 文字列が [7.](#7-かなkanaフィールドの文字制約) の対応文字集合に収まっているか
  （非対応文字が含まれると、その文字がタイプ不能になる）
- `kanji` と `kana` の区切り数・順序が意味的に対応しているか（機械的検証は不可能。
  少なくとも「セグメント数が想定と違わないか」「`kanji` を連結した文字列が意図した
  表示文と一致するか」はGUI側で機械チェック可能）
- `segments` 配列の要素が空文字列（`kanji`・`kana` とも `""`）だけで構成されて
  いないか
- フレーズ全体（`kana` を連結したもの）が空文字列になっていないか
- [9.](#9-レベルの目安と台本の連続性ルール) の文字数目安・台本の連続性ルールからの逸脱
  （これは機械的に警告する程度で、強制すべきではない）

## 12. サンプル

```json
{
  "Easy": [
    {
      "segments": [
        { "kanji": "企画部の", "kana": "きかくぶの", "mark": "normal" },
        { "kanji": "吉田",     "kana": "よしだ",     "mark": "strong" },
        { "kanji": "から",     "kana": "から",       "mark": null }
      ]
    },
    {
      "segments": [
        { "kanji": "20", "kana": "20", "mark": "normal" },
        { "kanji": "%",  "kana": "ぱーせんと", "mark": "strong" },
        { "kanji": "オフ", "kana": "おふ", "mark": null }
      ]
    }
  ],
  "Normal": [],
  "Hard": []
}
```

1つ目のフレーズから、ゲーム側は次を自動生成します:

- `text`（タイピング対象） = `"きかくぶのよしだから"`
- `displayText`（表示） = `"企画部の吉田から"`
- ローマ字入力欄・漢字混じり文の両方で、「吉田」（`よしだ`）の範囲だけ ▲（強く）
  マークが表示される

2つ目のフレーズのように、`kanji` と `kana` の文字数が大きく異なっていても
（`"%"` 1文字 に対し `"ぱーせんと"` 5文字）問題ありません。**セグメント単位で
意味が対応していれば良く、文字数を揃える必要はありません。**

## 13. GUIエディタへの実装上の推奨事項

- レベル（Easy/Normal/Hard）ごとにフレーズを一覧表示し、フレーズを選択すると
  セグメントの編集画面に入る、という2階層のUIを推奨します。
- フレーズ内のセグメントは、ドラッグで並べ替え可能なリストとし、各行に
  「かな」「漢字」「mark（プルダウン: 無指定／弱く／普通に／強く）」の3項目を
  入力できるようにしてください。
- 保存前に、[11.](#11-バリデーション) の「GUIエディタ側で保存前にチェックすることを
  推奨する内容」を検証し、問題があれば警告を出してください。特に **`kana` の
  文字集合チェック**（[7.](#7-かなkanaフィールドの文字制約)）は、対応漏れがあると
  ゲームがプレイ不能になるため優先度が高いです。
- 編集中は、全セグメントの `kana` / `kanji` を連結したプレビュー（`text` /
  `displayText` 相当）をリアルタイムに表示し、意図した文章になっているか目視
  確認できるようにしてください。
- 出力するJSONは2スペースインデントで pretty-print してください（既存データが
  この形式のため、diff レビューがしやすくなります）。
- スキーマ自体を変更する場合（フィールド追加など）は、本ドキュメントと
  `src/sentences.ts`（型定義・`validateSentencesData`・`seedsOf`）を対応して
  更新してください。
