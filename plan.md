# アナログキーボードを用いた打鍵圧センシングに基づくタイピングゲーム 実装仕様

> Scrapbox「アナログキーボードを用いた打鍵圧センシングに基づくタイピングゲーム」より

---

## 1. ゲーム概要

- 普通のタイピングゲームと同様に画面のお題を入力する
- 各フレーズには**打鍵圧の指示**が付与される
- **基本評価**: タイピング速度（フレーズ完了ごとに 100pt）
- **ボーナス評価**: 指示通りの打鍵圧でクリアできれば +50pt

---

## 2. 画面仕様

### 2-1. スタート画面

| 要素 | 内容 |
|------|------|
| タイトル | ゲーム名（仮称: keywave） |
| 接続ボタン | アナログキーボード（AnalogSense）接続用ボタン |
| 開始案内 | "Press any key" の文字 |

- 何かキーを押すとゲーム画面へ遷移

---

### 2-2. ゲーム画面

#### お題表示
- タイピングお題が吹き出し形式で**縦に複数段**表示される
- プレイヤーは**一番下のフレーズ**を入力する
- 入力完了すると吹き出しが上にスクロールし、次のフレーズが一番下に来る

#### 打鍵圧フィードバック（横顔キャラクター）
- 画面**左下**に 300° の弧で描かれた横顔が表示される
- 最後に打鍵された打鍵圧の強さに応じて弧の角度が変化する
  - 打鍵圧 **強**: 300°（口が閉じた状態）
  - 打鍵圧 **弱**: 350°（口が開いた状態）
- **フォルマント合成**により打鍵圧に応じた音声フィードバックを出す
  - 強: 「ワ」の母音
  - 弱: 「ウ」の母音
  - 中間は線形補完

#### 打鍵圧指示アイコン（吹き出し左上に表示）
各フレーズには以下3種類のいずれかの指示が付く（ランダム割り当て）：

| 種類 | アイコン | 色 | クリア条件 |
|------|--------|-----|-----------|
| だんだん強く | 右肩上がりの三角形 | 赤 | フレーズ内の **70%以上** のローマ字が、直前のローマ字より強く打鍵されている |
| だんだん弱く | 右肩下がりの三角形 | 青 | フレーズ内の **70%以上** のローマ字が、直前のローマ字より弱く打鍵されている |
| ビブラート | 波線 | 緑 | フレーズの **80%以上** のローマ字が、直前のローマ字と打鍵圧の絶対差 **2以上** ある |

#### ゲーム時間
- **2分間**でゲーム終了 → リザルト画面へ

---

### 2-3. リザルト画面

#### スコア表示
```
{タイプしたフレーズ数 × 100} + {打鍵圧クリア回数 × 50} = {スコア}
```

例: フレーズ 10 個タイプ、うち 6 個打鍵圧クリア → `10×100 + 6×50 = 1300pt`

| 要素 | 内容 |
|------|------|
| スコア計算式 | 上記の形式で表示 |
| タイトルへ戻るボタン | 画面左下に配置 |

---

## 3. 技術スタック

| 項目 | 内容 |
|------|------|
| フレームワーク | Vite + TypeScript |
| デバイス連携 | AnalogSense（`AnalogsenseHandler.ts`） |
| 打鍵圧推定 | `calcPressure.ts`（速度ベースの運動量モデル） |
| タイピング判定 | `keygraph.js`（`keygraph.build(ひらがな)` でお題セット、`keygraph.next(key)` で正誤判定） |
| 音声合成 | **Tone.js v15**（`tone` パッケージ・フォルマント合成） |
| 描画 | Canvas API または SVG（横顔キャラクター・要実装） |

### keygraph.js の使い方（主要 API）

```ts
import { keygraph } from "./keygraph.js";

keygraph.build("ひっしゅう");         // お題をひらがなでセット
keygraph.key_candidate();             // 次に打つべきキー列（例: "hisshu"）
keygraph.seq_done();                  // 打ち終わったひらがな
keygraph.seq_candidates();            // これから打つひらがな

// keydown イベントで
if (keygraph.next(e.key)) {
    // 正解
    if (keygraph.is_finished()) { /* フレーズ完了 */ }
} else {
    // 不正解（ミス）
}
```

- お題はひらがなで渡す（ローマ字変換は keygraph が内部処理）
- 複数ローマ字候補（例: 「し」→ "si" / "shi"）は自動で対応
- `key_done()` で打ったキー列履歴が取れる（打鍵圧記録との対応付けに利用）

### Tone.js によるフォルマント合成

打鍵圧（N値）を 0〜1 に正規化した `t` を使い、「ウ」と「ワ」の中間音を線形補完して出す。

```ts
import * as Tone from "tone";

// 「ウ」のフォルマント周波数（F1, F2, F3 の概略値）
const U_formants = [300, 700, 2300];   // Hz
// 「ワ」のフォルマント周波数
const WA_formants = [800, 1200, 2600]; // Hz

// t=0: 「ウ」（弱打鍵）, t=1: 「ワ」（強打鍵）
function playFormant(t: number) {
    const freqs = U_formants.map((f, i) => f + (WA_formants[i] - f) * t);
    // Tone.js の Filter + Noise、または PlayerSynth で実装
}
```

実装の選択肢:
- `Tone.Filter` + `Tone.Noise`（帯域通過フィルターでフォルマントを模倣）
- `Tone.AMSynth` / `Tone.FMSynth`（より手軽だが厳密なフォルマントではない）

---

## 4. 打鍵圧推定モデル（calcPressure.ts 現状）

```
F_average = (m × v²) / (2d)
```

- `m` = 0.002 kg（キーと指の質量）
- `v` = strokeMM / 打鍵時間（ms）　← 平均速度
- `d` = 0.001 m（仮の距離）
- 押し切り前に戻った場合は `(1 - value) × 0.4 + value × 0.6` でニュートン値を補間

**コールバック**: `SetPressureCallback((code, newtonValue) => {...})` で圧力値を受け取る

---

## 5. 未実装コンポーネント（実装タスク）

- [ ] スタート画面 UI
- [ ] フレーズ管理（お題リスト・吹き出し表示・ローマ字変換）
- [ ] キー入力処理とフレーズ判定ロジック
- [ ] 打鍵圧指示のランダム割り当て
- [ ] 打鍵圧クリア判定（だんだん強く / だんだん弱く / ビブラート）
- [ ] 横顔キャラクターの描画（弧の角度変化）
- [ ] フォルマント合成音声フィードバック
- [ ] 2分タイマー
- [ ] リザルト画面とスコア計算
- [ ] 画面遷移管理（スタート → ゲーム → リザルト）

---

## 6. 参考情報

- Scrapbox「打鍵圧センシングに基づくタイピングゲーム 仕様」: 没案（現在の案は上記ページ）
- 研究概要: VibeTyping / ExpressiveTyping 的な方向性
- 実験計画: 圧力センサ実測値とモデル推定値の比較が必要
